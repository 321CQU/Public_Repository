[tool.poetry]
name = "321cqu-utils"
version = "0.1.0"
description = ""
authors = ["321CQU Developers"]
license = "AGPL v3"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
grpcio = ">=1.57.0"
prometheus-client = ">=0.20.0"
protobuf = ">=4.24.2"
pydantic = ">=2.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
